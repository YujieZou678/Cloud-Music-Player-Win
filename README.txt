 /***************************************************************************/
 开发文档：
 
 //何年何月何日
 1.数据和图片的缓冲界面。
 2.实现最大同时三个网络异步请求(数量可调，加代码的问题)。
 
 //2023.11.17
 1.网络请求失败，拿不到数据就手动给模块赋值或不给模块赋值，并且报错处理，并且定时反复请求（仅在推荐窗口）。
 
 //2023.11.18
 1.使用git管理，并拥有远程仓库。
 2.列表双击播放音乐。
 3.图片缓冲加了进度%。
 
 问题: qml的Image图片处理，自动网络请求，网络路径不同，有的会缓存有的不会，有的会自动重新加载有的不会？
 2023.11.19 答：上述问题是因为PathView可视的delegate/item会重新加载，不可视就不加载，导致了切换图片会重新给路径赋值即重新加载。
 
 //2023.11.19
 1.图片加载失败，赋予一个默认路径，不自动重复请求。
 
 想法：一般因为网络加载失败了，然后网络又好了，可以手动刷新，自动反复请求的话对后台消耗太大（非必要内容不加该功能）。
 
 //2023.11.20
 1.新增了requestNetwork.js文件，放入网络请求模板函数，实现模块化编程。之前失败应该是因为qml导入js文件命名没有注意首字母必须大写。
 2.bug,由列表和新歌视图里的index引起的问题。
 3.搜索添加默认搜索。
 4.主窗口空格快捷键，播放与暂停。
 5.播放滑块缓冲动画。
 6.切换播放逻辑处理。
 
 //2023.11.21
 1.js文件放入播放函数，模块化编程。
 2.实现列表自动顺序播放。
 
 问题：有个bug，拖动，有延迟，应该是内部position赋值之后继续计时，但是没有实时传出数据。内部问题。猜测：MediaPlayer播放作为子线程和主线程不同步，主线程阻塞？不晓得咋解决！也有可能是网络传输的问题。
 
 //2023.11.22
 1.解决小bug,同一个列表的循环播放。
 2.当前正在播放的列表界面，不销毁，不覆盖。专辑/歌单界面，切换歌单，原正播放的歌单，应该是独立的。(2024.1.31 该设计很好，两个视图交互使用，以保留前一个视图。谁播放谁独立。)
 
 //2023.11.23
 1.搜索界面光标的问题。
 2.上一首和下一首，按钮+快捷键(ctrl+alt+左和ctrl+alt+右)
 3.进度条栏，新增图片信息。
 4.js文件新增Json数组格式化处理函数，用于列表数据，模块化编程。
 5.播放模式的切换。顺序播放，循环播放，随机播放。逻辑：两个列表数组，播放历史列表，当前歌单列表。
 
 //2023.11.25
 1.关于上一首和下一首的逻辑问题：
     {
     //版本1（初始版本，上一首为播放历史列表，已删除）
     首先，有两个列表，分别是播放历史列表(相当于当前的播放列表)和歌单列表，两列表分别独立。
     播放历史列表，包括历史和当前播放的歌，当前播放的歌在末尾；歌单列表就是为了赋值一首歌增加到历史列表末尾进行播放。
     切歌分为三部分，得到index值，播放，高光块显示。
     特殊情况：index位于播放历史列表中间，而不是末尾，点击一首新歌，此时播放路径会分叉。
     注意：上述为总体逻辑，中间会有其他细节处理。整体难点就是不知道逻辑该是怎么样的。
     }
     {
     //2024.2.13 版本2
     参考qq音乐逻辑。
     顺序模式：上一首和下一首只是用于切换当前歌单列表。
     随机模式：上一首为播放历史列表（仅是当前歌单列表里随机播放的历史列表，应独立于总的播放历史列表），如没有则依然为随机。
     循环模式：手动切歌同顺序模式。
     播放历史的问题：只会存在一首歌，如：播放了历史列表的歌曲，那么不会重复添加它，而是被置顶。（这其实增加了复杂度，每播放一首歌就需要遍历历史列表）
     }
   期间遇到的问题(就是对JS不熟)：
     slice()：不会改变数组本身，只能赋值，如arr = arr.slice()
     push() splice()：两者会改变数组，直接执行，如arr.push() arr.splice()，它们本身返回的值反而不一样。
     push()不会触发onChange信号。如arr[]，arr.push()，不会触发信号，只能等值替代。
     concat()合并。
     
 //2023.11.26
 1.完善解决播放模式的切换，上一首和下一首的问题。
 
 //2024.1.21
 开始寒假了，继续完善去年的项目O.o。
 
 1.本地音乐起了个头。
 
 //2024.1.27
 1.自动查找本地音乐某个文件夹，并得到数据。
 2.升级第一条，遍历某个文件夹的所有音乐。需要算法。
 
 //2024.1.28
 1.通过C++代码实现昨天的第二条。QDirIterator可直接得到需要的文件，如果用QDir也可自己通过递归得到。
 
 //2024.1.29
 1.本地音乐搜索应该使用多线程和信号与槽机制，否则会影响主线程如：没有加载界面。
 
 //2024.1.30
 1.本地音乐搜索实现子线程查找。子线程与主线程在信号与槽之间传递参数。
 2.本地音乐能正常播放，但是界面逻辑有点小bug。
 
 //2024.1.31
 1.解决本地音乐播放缓冲界面不消失的bug。
 2.解决本地音乐播放列表高亮块不切换的bug。
 3.本地音乐播放后，因为没有歌词会出现问题。(本地音乐歌词，后期再说)
 
 //2024.2.3
 1.解决一个bug，连续两次点同一个专辑/歌单的情况(targetID没变，不会网络请求，界面逻辑出问题)。
 2.手动添加本地音乐应该检查是否重复。
 
 //2024.2.4
 1.完善并优化本地音乐列表去重。
 
 //2024.2.5
 1.qml保存数据。(qml的settings用不了，原因是QJSValue已不再被使用来传输数据，只能用C++代码靠QVariant实现)
 2.已实现上述的数据缓存，QSettings, QVariant, QVariantMap。
 3.本地音乐缓存数据列表的顺序会变, 已实现排序(但是因为这个给qml的数据类型改为了QVariantMap)。//2024.2.14 不需要排序了。
 
 //2024.2.14
 1.切歌逻辑重写，参考了qq音乐。（顺序播放）
 2.播放历史模块基本完善。依然是缓存数据顺序问题还没解决。
 3.解决上述的缓存.ini文件顺序问题。
 
 //2024.2.15
 1.随机播放逻辑重写完成。（目前测试无bug）
 
 //2024.2.16
 1.我喜欢板块基本完成。
 
 //2024.2.17
 1.我喜欢板块测试完成，已修复目前发现的逻辑bug。(我喜欢的歌曲要保证是同一个对象；需要刷新)
 
 //2024.2.21
 1.实现初步美化，一改工业风。
 2.实现背景改变的无缝衔接。
 3.解决播放历史没替换收藏对象的bug。
 4.播放历史有bug。需要运用到JSON对象的深拷贝。
 
 //2024.2.22
 1.将清除缓存和刷新合并。
 {
    2.我喜欢清除后数据不能同步的bug解决。
    3.跟播放历史实现统一对象。
    4.我喜欢和播放历史的颠倒不能是副本。
  目前类似这样的逻辑bug已解决。
 }
 
 5.底部的收藏按钮还未实现。
 6.mainAllMusicList好像有点问题，导致切歌有问题，明天再看。
 
 2024.2.23
 1.昨天的mainAllMusicList不是副本，会同步变化导致的问题。
 2.而实现底部收藏按钮则需要的mainAllMusicList不能是副本。
 3.底部收藏按钮功能实现。
 4.解决切歌如果是同一首歌不会重新播放的bug。
 5.解决推荐内容视图的逻辑bug。如：bannerList和gridLatestList的收藏及播放问题。
 
 2024.2.29
 1.窗口去边框后的全套处理，包括：移动，放大，手动改变窗口大小。参考：详细见CSDN我的收藏2024, 博客"最简单的方式实现QML无边框窗口边缘拖动调整大小"。
 
 2024.3.1
 1.实现窗口去边框并有相应的功能。
 2.完善放大缩小窗口，组件大小不一致的bug。
 
 2024.3.2
 1.完善小窗播放功能。
 2.解决小窗播放后背景切换的bug, 小窗后放大的bug。
 3.增加系统托盘的功能。
 4.增加托盘菜单图标。有bug：右键例如系统托盘，然后拖动窗口，当单点击后会一直得到移动焦点而不会取消；是window.startSystemMove()内部的问题，目前解决不了。
 
 2024.3.4
 1.增加功能：歌词界面鼠标悬停在左半部分3秒钟，会连带top和bottom隐藏，并已完善与之相关的逻辑处理。
 2.自定义Tooltip。
 
 2024.3.9
 1.自上次提交的版本后都是测试阶段（修复一些bug）。
 
 2024.3.12
 1.mainAllMusicListCopy没必要，已删除。
 2.解决底部我喜欢按钮的同步问题。
 
 2024.4.6
 1.云服务器更换。
 
 2024.4.8
 1.搜索模块，歌单模块，专辑模块，本地音乐，新歌推荐模块增加mv属性。
 
 2024.4.10
 1.MV功能实现完毕。
 2.网络请求超时的处理。（有现成的函数调用，如：manager[0]->setTransferTimeout(10)）
 
 /***************************************************************************/
       
 当前需要完成的功能：

 
 /***********************************/
 技术栈(笔记归纳)：
 C++:
 
 QDirIterator: 遍历文件夹的文件包括子文件夹
 QDir, QFileInfo
 std::sort(): 升序和降序
 QThread, moveToThread()。Qt实现多线程有几种方式，详细见CSDN我的收藏，子线程与主线程在信号与槽之间传递参数。
 RAII结构，构造函数内申请堆对象，析构函数手动释放。
 析构不允许发生异常，如果有异常就吞掉。
 C++读取文件：fstream, ifstream, cin>>, getline()。
 C++读取文件(Qt版)：QFile, QByteArray, QTextStream, 
 qml的JS数据如{id: "123}在C++这边为QVariantMap，至于QJSValue已不再使用于传输数据，故目前的qml的settings组件使用不了，而替代它的C++代码如下：
 QSettings: settings->beginGroup("localMusic");  //进入localMusic目录。注意该类共用一个settings，记得退出！！！
            settings->endGroup();  //退出localMusic目录
            在这里上当了，务必注意！！！
 QVariant: 几乎万能的数据类型，也是目前推广的。如：qml和C++之间的数据都可用它来传递。
 QVariantMap
 
 Qml:
 
 ListView: ListModel, FolderListModel
 FileDialog
 FolderDialog
 本地url的格式: "file:///..."
 split 分，join 合
 pop() 后, shift() 前。
 unshift('a'): 在数组最前面插入值。
 concat 合并两数组并返回新数组的值而不改变原数组。
 list.forEach(item=>{}): 遍历数组
 slice()。不会改变数组本身。截取。
 splice：会改变原数组。截取。
 reverse：颠倒数组，会改变原数组。
 FolderListModel: folder和count的关系
 
 
 
 
 /***********************************/
 
